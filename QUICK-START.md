# 🚀 راهنمای سریع اتصال و اجرا روی سرور

## 📝 اتصال به سرور

### روش 1: استفاده از SSH (پیشنهادی)

در **Terminal** یا **PowerShell** یا **CMD** دستور زیر را بزن:

```bash
ssh root@82.115.20.113
```

**بعد از زدن Enter:**
- یک پیام می‌آید که می‌پرسد "Are you sure you want to continue connecting (yes/no)?"
- تایپ کن: `yes` و Enter بزن
- بعد می‌پرسد: `root@82.115.20.113's password:`
- **پسورد را وارد کن:** `NUx72Zm4kppkkwxi`
- ⚠️ **نکته مهم:** وقتی پسورد را تایپ می‌کنی، هیچ چیزی نمایش داده نمی‌شود (نه ستاره، نه نقطه) - این عادی است!
- بعد از وارد کردن پسورد، Enter بزن

### روش 2: استفاده از Putty (ویندوز)

1. Putty را باز کن
2. در Host Name: `82.115.20.113` وارد کن
3. Port: `22` (پیش‌فرض)
4. Connection Type: `SSH`
5. روی Open کلیک کن
6. وقتی پیام آمد، Yes بزن
7. Username: `root`
8. Password: `NUx72Zm4kppkkwxi` (هنگام تایپ نمایش داده نمی‌شود)

---

## 📦 نصب و راه‌اندازی

بعد از اتصال به سرور، این دستورات را یکی یکی اجرا کن:

```bash
# 1. Clone کردن پروژه
git clone https://github.com/webmasterbartar/maps-scrapper.git
cd maps-scrapper

# 2. نصب dependencies
chmod +x deploy.sh
./deploy.sh

# 3. اجرای اسکرپر (در background)
chmod +x start-scraper.sh
./start-scraper.sh
```

---

## 👀 مشاهده لاگ‌ها

```bash
# مشاهده لاگ زنده
tail -f logs/scraper_*.log

# یا آخرین لاگ
ls -lt logs/ | head -2
tail -f logs/$(ls -t logs/ | head -1)
```

---

## 🛑 توقف اسکرپر

```bash
./stop-scraper.sh
```

---

## ✅ بررسی وضعیت

```bash
# بررسی اینکه آیا در حال اجرا است
ps aux | grep "node src/index.js"

# بررسی PID
cat scraper.pid
```

---

## ⚠️ نکات مهم

1. **پسورد هنگام تایپ نمایش داده نمی‌شود** - این عادی است!
2. بعد از `./start-scraper.sh` می‌توانی SSH را قطع کنی - برنامه ادامه می‌دهد
3. خروجی‌ها در `output/` ذخیره می‌شوند
4. لاگ‌ها در `logs/` ذخیره می‌شوند

---

## 🔧 اگر مشکل داشتی

### مشکل: "Permission denied"
```bash
chmod +x *.sh
```

### مشکل: "Command not found: git"
```bash
apt-get update
apt-get install -y git
```

### مشکل: "Command not found: node"
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt-get install -y nodejs
```

